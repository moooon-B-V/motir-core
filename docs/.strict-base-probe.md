# Strict-base probe (throwaway)

Temporary artifact for MOTIR-2003: proves that a PR whose base is stale is blocked
from merging until it is updated, after `protect-main` gained a strict
`required_status_checks` rule. This branch and its PR are deleted immediately after
the check; nothing here is meant to merge.
