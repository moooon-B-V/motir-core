# Throwaway probe (do not merge)

Exists only to give the `docs/ci-complete-probe` branch a commit of its own, so its
CI run does not share a SHA — and therefore a check-run list — with
`subtask/MOTIR-2008-ci-complete-gate`. Deleted with the branch.
